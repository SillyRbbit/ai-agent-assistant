# Post-increment gate

```text
Use $post-increment-gate for the active increment.

Confirm the expected gate state, run the structured session-end inventory, execute every required check, and classify all automated and manual evidence. Perform architecture, security, code-health, technical-debt, and readiness reviews before synchronizing project memory.

Create the dated consolidated report from docs/templates/POST_INCREMENT_REVIEW_TEMPLATE.md. Return exactly PASS, PASS WITH ADVISORIES, or FAIL. Finalize the marker only for a non-blocking result and require status complete with valid true. Do not fix advisories automatically, commit, push, merge, publish, or start another increment.
```
