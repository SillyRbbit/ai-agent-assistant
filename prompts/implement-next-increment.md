# Implement the next increment

```text
Implement only the first item marked Ready in NEXT_STEPS.md.

Start by reading AGENTS.md, HANDOFF.md, PROJECT_STATUS.md, DECISIONS.md, SECURITY.md, CODE_REVIEW.md, and the relevant product and increment documentation. State the goal, explicit non-goals, expected files, security considerations, and verification commands before editing.

Requirements:
- Preserve existing behavior outside the increment.
- Keep portable logic separate from platform-specific code.
- Use strict TypeScript and typed Rust errors.
- Do not use unwrap, expect, panic, todo, or unimplemented in production Rust paths.
- Add focused tests for success and failure behavior.
- Do not add a production dependency or capability without a documented decision.
- Do not introduce API keys, unrestricted shell access, broad filesystem access, Accessibility, screen capture, Apple Events, or production network calls.

After implementation, run the relevant checks, inspect the diff against CODE_REVIEW.md and SECURITY.md, update the increment record and all project-memory files, and report passed, failed, and not-run checks separately.
```
