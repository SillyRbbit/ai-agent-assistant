import { PlaceholderPage } from "../shared/PlaceholderPage";

export function TasksPage() {
  return (
    <PlaceholderPage
      description="A future dashboard for local tasks and reviewed reminders."
      emptyDescription="Task creation and persistence are intentionally deferred."
      emptyTitle="No tasks yet"
      headingId="tasks-page-title"
      icon="T"
      title="Tasks"
    />
  );
}
