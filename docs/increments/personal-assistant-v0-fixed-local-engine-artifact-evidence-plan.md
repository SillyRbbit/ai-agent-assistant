# Personal Assistant V0 fixed local engine and artifact evidence plan

Status: Complete — **PASS WITH ADVISORIES**; owner accepted
`candidate_not_eligible_or_unproven` and exact D-121; the completion marker is
valid; every operational successor remains Blocked
Owner: Project owner
Date: 2026-09-03
Baseline: `355d42ac8bb9a5ed663f57895675df688507ce9f`
Branch: `codex/personal-assistant-v0-fixed-local-engine-artifact-evidence-plan`
Gate: `personal-assistant-v0-fixed-local-engine-artifact-evidence-plan`
Plan:
[`docs/plans/2026-09-03-personal-assistant-v0-fixed-local-engine-artifact-evidence-plan.md`](../plans/2026-09-03-personal-assistant-v0-fixed-local-engine-artifact-evidence-plan.md)

## Objective

Assess, through staged documentation evidence only, whether one exact future
application-owned, fixed, nonselectable, in-process local/no-auth candidate for
`real-content-v2` could support a separately approved target-Mac evidence plan.
No candidate is selected or admitted by this increment.

## Current checkpoint

The owner approved the exact Ready plan, its sixteen-file ceiling, the branch,
the gate, and work through the candidate-nomination-envelope checkpoint. The
owner later authorized one same-active-increment documentation-only amendment:
current official public primary-source research solely to propose exactly one
security-first fixed in-process engine/model envelope before acceptance.

Repository state was reconfirmed before `begin`:

- `HEAD`, local `main`, and the locally recorded `origin/main` were exactly
  `355d42ac8bb9a5ed663f57895675df688507ce9f`;
- ahead/behind was `0/0`;
- the approved plan was the sole workspace change;
- the completed predecessor remained identified as
  `personal-assistant-v0-pr112-publication-closeout`; and
- D-121 plus the exact increment and review paths were unused.

The approved branch was created and
`python3 .codex/hooks/post_increment_gate.py begin --increment personal-assistant-v0-fixed-local-engine-artifact-evidence-plan`
ran exactly once. Gate status then identified this increment as active.

The owner subsequently accepted the exact immutable envelope and separately
authorized the then-active increment to perform its full documentation-only
static evidence assessment. `begin` was not rerun. The evidence-resolved tuple
and conjunctive matrix were complete, and work paused before final
reconciliation or completion gates.

The owner then explicitly accepted the matching tuple, completed fifteen-row
matrix, closed `candidate_not_eligible_or_unproven` disposition, and exact
D-121 wording. The then-active increment was authorized to perform only its
documented sixteen-file final reconciliation and completion gates. No
operational or successor authority followed.

The exact sixteen-file reconciliation and all prescribed completion checks are
now complete with **PASS WITH ADVISORIES** and a valid completion marker. The
increment is stopped for owner review; no successor is selected or Ready.

## Accepted nomination envelope

The bounded amendment relied only on the official public evidence pages frozen
in the plan; it did not download source archives, binaries, packages, or model
artifacts and did not resolve dependencies, build, execute, inspect the target
Mac, or access an operational system. The owner accepted exactly this immutable
envelope:

| Group               | Owner-accepted frozen value                                                                                                                                                                                                                                                                                                                                                         |
| ------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Governance identity | `fixed_local_v2_llamacpp_0_3_0_qwen2_5_1_5b_q4km_cpu_v1`; documentation-only fixed `real-content-v2` assessment candidate; no selector, D-119 handle, admission, retry, or fallback                                                                                                                                                                                                 |
| Engine              | `ggml-org/llama.cpp`; release label `v0.3.0`; controlling full commit `c1d0e7a004015f23bc0233470b747b596f29b264`; the immutable commit controls over the mutable release or “latest” label                                                                                                                                                                                          |
| Model               | `Qwen/Qwen2.5-1.5B-Instruct-GGUF` at `91cad51170dc346986eccefdc2dd33a9da36ead9`; sole intended `qwen2.5-1.5b-instruct-q4_k_m.gguf`; publisher metadata SHA-256 `6a1a2eb6d15622bf3c96857206351ba97e1af16c30d7a74ee38970e434e9407e`, size `1,117,320,736` bytes; neither value is locally verified                                                                                    |
| Topology            | Statically linked in-process `llama` C API, CPU backend only; Metal, Accelerate/BLAS, RPC, dynamic backend discovery/loading, server, CLI, tools/examples/app, multimodal, HTTPS/curl/OpenSSL, helper/subprocess, plugin, JIT/dynamic code, alternate backend, retry, and fallback forbidden; exact build and symbol closure unproved                                               |
| Target class        | Sanitized macOS 14.0+, arm64 Apple Silicon, minimum 16 GiB unified memory and 8 GiB available disk; Apple GPU unused; no device/account identifier and no target observation                                                                                                                                                                                                        |
| Acquisition         | `pre_provisioned` only into one future Rust-owned fixed slot under separate authority; no runtime download/update/repair, file chooser, caller path, alternate source, cache search, or bundled redistribution                                                                                                                                                                      |
| Sources             | Exact official engine release/commit, immutable `github.com/ggml-org/llama.cpp` tree/blob and `raw.githubusercontent.com` source-text boundaries, GitHub current-release metadata/security pages, exact immutable Qwen revision/metadata pages, and the versioned Qwen guide, bounded by the exact project, commit, pages, and immutable descendant prefixes enumerated in the plan |

The proposed future artifact locator is recorded in the plan but excluded from
the evidence-access allowlist. It may not be followed without a later exact
artifact-acquisition plan. The proposed build-option closure is also recorded
in the plan and remains unexecuted and unproved.

Official evidence supports only nomination: the engine offers an in-process C
API, its pinned header documents decode cancellation as CPU-only, and the
publisher lists the one instruction-tuned Q4_K_M artifact under Apache-2.0.
Pinned engine source also exposes a material unresolved dynamic-loader
capability and broad excluded surfaces. If the later full assessment cannot
prove every forbidden capability absent or unreachable, the candidate fails
closed; no source patch or substitute is authorized.

The accepted envelope authorized no candidate selection, admission, artifact
access, or implementation. The owner separately authorized only the frozen
allowlist static assessment recorded below.

## Full static evidence checkpoint

The full static assessment used only the accepted envelope's frozen official
primary-source allowlist. Evidence was captured at `2026-09-04T03:11:43Z` UTC.
One repeat request for the frozen Qwen README returned HTTP 429; no alternate
endpoint, raw/download workaround, artifact redirect, or wider source was used.
No source archive, binary, package, model byte, artifact object, credential,
provider, signing system, target Mac, or other operational system was accessed.

Disposable metadata-only dependency resolution was Not run. The envelope names
the direct C/C++ source and C API but no Rust binding or registry package; adding
one merely to resolve it would change the accepted identity. Resolution also
could not cure the decisive pinned-source dynamic-loader incompatibility.

### Evidence-resolved tuple

| Tuple component       | Status              | Static result                                                                                                                                                                                                    |
| --------------------- | ------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Governance identity   | `documented`        | Candidate ID, fixed `real-content-v2` purpose, no selector, no D-119 handle, and no admission remain exact.                                                                                                      |
| Engine source         | `contract_unproven` | `v0.3.0` resolves to `c1d0e7a004015f23bc0233470b747b596f29b264`, but no separately verified source-tree/archive digest or final source inventory exists.                                                         |
| Engine build          | `contract_unproven` | The intended flags do not remove the unconditionally compiled backend loader/registry, and no configured cache, object inventory, link map, or symbol table exists.                                              |
| Model identity        | `contract_unproven` | The frozen project, revision, filename, publisher-listed digest, and byte count are documented; embedded GGUF identity, tokenizer, template, tensor inventory, and exact parser compatibility remain unverified. |
| Artifact manifest     | `contract_unproven` | Publisher metadata is not a locally verified same-byte manifest, and exact auxiliary-input independence is unproved.                                                                                             |
| Provenance and rights | `contract_unproven` | Engine MIT and model Apache-2.0 are documented, but exact source-model-to-GGUF conversion provenance, notices, signatures, attestations, and redistribution classification are incomplete.                       |
| Acquisition           | `contract_unproven` | `pre_provisioned` is frozen, but source transport, staging, fixed-slot checks, verifier-before-parser ordering, promotion, rollback, and quarantine are unspecified.                                             |
| Removal               | `contract_unproven` | No authoritative file/cache/temp inventory, mapped-memory handling, deletion ordering, or no-repair/no-redownload proof exists.                                                                                  |
| Resource envelope     | `contract_unproven` | Artifact bytes and publisher context claims are known, but RAM, KV-cache, disk staging, threads, latency, thermal, and teardown bounds are not.                                                                  |
| Target compatibility  | `not_run`           | No target-Mac build, load, inference, resource, cancellation, cleanup, or no-egress observation occurred.                                                                                                        |

### Conjunctive static evidence matrix

| Required row               | Status              | Evidence-resolved result                                                                                                                                                                                          |
| -------------------------- | ------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Exact engine identity      | `contract_unproven` | Commit identity is fixed, but complete source and final native artifact identity are not verified.                                                                                                                |
| Exact model identity       | `contract_unproven` | Publisher coordinates are fixed, but the artifact bytes and embedded GGUF identity were not accessed or parsed.                                                                                                   |
| Dependency closure         | `contract_unproven` | Pinned source unconditionally compiles `ggml-backend-dl.cpp` and `ggml-backend-reg.cpp`; `GGML_BACKEND_DL=OFF` does not remove public `ggml_backend_load*` entry points or POSIX `dlopen`/`dlsym` implementation. |
| Provenance and licensing   | `contract_unproven` | Published licenses are visible, but the exact conversion chain, inherited notices, attestations, and Cortexa distribution treatment are incomplete.                                                               |
| Artifact verification      | `contract_unproven` | Publisher digest and byte count were not compared to artifact bytes; parser-first avoidance and same-byte loader binding are unproved.                                                                            |
| Acquisition and removal    | `contract_unproven` | No bounded fixed-slot acquisition, rollback, removal, mapped-memory, or orphan-cleanup contract is established.                                                                                                   |
| Complete no-egress         | `contract_unproven` | Broad network surfaces appear flag-gated, but no final build was inspected and the surviving arbitrary-path native loader remains a configurable code-loading edge.                                               |
| Native TCB reachability    | `contract_unproven` | Same-process native code retains filesystem, mmap, environment, logging, and dynamic-library reach; absence of credential or ambient authority is not proved.                                                     |
| Streaming and bounds       | `contract_unproven` | Caller-driven incremental decode is possible, but no bounded DTO stream, backpressure, UTF-8 delta, queue, token, RAM, disk, or timing contract exists.                                                           |
| Deadlines and cancellation | `contract_unproven` | Load and CPU decode callbacks exist, but there is no all-phase hard deadline, polling-latency bound, bounded join, or guaranteed interruption of a running native operation.                                      |
| Cleanup and quarantine     | `contract_unproven` | Free APIs exist, but upstream source states dynamic backends cannot safely unload while threads may retain library resources; bounded teardown and positive quiescence are absent.                                |
| Late-result rejection      | `contract_unproven` | The synchronous C API carries no Rust-issued run/attempt/request/generation/event sequence; pre-mutation stale-result rejection requires a separately proved ingress boundary.                                    |
| Owner authentication       | `contract_unproven` | Provider-free execution does not satisfy D-062/D-094 Cortexa owner authentication.                                                                                                                                |
| Personal-data lifecycle    | `contract_unproven` | No accepted prompt/output/model-buffer retention, zeroization, logging, crash, swap, or diagnostic-data contract exists.                                                                                          |
| Target-Mac protocol        | `not_run`           | Build, symbols, dependency closure, inference, limits, cancellation latency, bounded join, quiescence, egress, and compatibility were intentionally not run.                                                      |

Matrix totals are zero `documented`, fourteen `contract_unproven`, one
`not_run`, and zero `boundary_failed`. The frozen coordinates and
source boundary remained intact; the negative result is therefore a candidate
property/evidence insufficiency, not a custody or scope violation.

Exactly one closed disposition is accepted:
`candidate_not_eligible_or_unproven`. Final reconciliation appends the exact
owner-accepted D-121 wording to `DECISIONS.md` without modifying any preceding
decision bytes.

## Preserved boundaries

- Synthetic-v1 remains the fixed blocked OpenAI-through-Cloudflare proof.
- `real-content-v2` remains fixed and nonselectable.
- Historical V0-14 remains unchanged and Blocked with its V0-13 dependency.
- D-118 remains `no_eligible_client`.
- D-119 retains exactly ten `candidate_blocked` entries and no handle; fixed
  local-v2 is not admission of `local_no_auth`.
- All ten D-107 blockers remain unproved; D-113 through D-117 remain Proposed
  and non-controlling.
- D-060/D-061 remain controlling for cloud paths, and D-062/D-094 Cortexa owner
  authentication remains independently mandatory and Blocked.
- Empty tools, explicit foreground action, one process-wide request, no retry
  or fallback, no-egress, untrusted-artifact handling, bounded streaming and
  resources, cancellation, bounded join, cleanup/quarantine through positive
  quiescence, pre-mutation late-result rejection, volatility, and no device
  effect remain mandatory.

## Work performed

- Passed: exact baseline, ahead/behind, branch, plan-only scope, predecessor,
  and path-availability inspection.
- Passed: creation of the approved `codex/` branch.
- Passed: the exact single `begin` command.
- Passed: checkpoint `npm run docs:check`, `npm run repository:check`,
  `npm run security:scan`, `git diff --check`, exact two-file scope, baseline
  ancestry, and active-gate status.
- Passed: one owner-authorized nomination-only current official public
  primary-source review; discovery used only public official owner/publisher
  pages, and the final candidate evidence is frozen to the plan's closed
  boundary.
- Passed: exactly one proposed complete immutable nomination envelope recorded
  in this increment and the plan.
- Passed: explicit owner acceptance of the exact immutable envelope.
- Passed: separate authority to continue the same active increment without a
  second `begin` and complete only the frozen-allowlist static assessment.
- Passed: matching ten-component evidence-resolved tuple and fifteen-row
  conjunctive matrix completed within the accepted source boundary.
- Passed: post-assessment formatting, `npm run docs:check`,
  `npm run repository:check`, `npm run security:scan`, `git diff --check`, exact
  two-file scope, and active-gate status.
- Passed: explicit owner acceptance of the sole
  `candidate_not_eligible_or_unproven` disposition and exact D-121 wording.
- Passed: exact fifteen-pre-report-path reconciliation, including append-only
  D-121, without selecting a replacement or changing an operational boundary.
- Passed: complete repository verification, exact-scope and preservation
  assertions, independent architecture/security/documentation/code-health/
  technical-debt/readiness review, session inventory, report validation, and
  the exact post-increment finalizer. The completion marker is valid.
- Not run: source/archive or artifact-byte retrieval, disposable dependency
  resolution, candidate selection/admission or installation,
  product/model-artifact filesystem work, source/dependency changes,
  candidate/source-engine build/load/inference, target-Mac inference, owner
  authentication, credentials, providers, signing, product systems, and
  operational external systems. Repository tests and application/Tauri
  verification builds passed.

## Stop reason

The owner-accepted static result and exact D-121 are reconciled across the exact
sixteen documentation paths. Required verification, independent review, the
consolidated report, finalizer, and valid completion marker passed. The
increment stops for owner review. Commit, push, merge, replacement, artifact,
target-Mac, source, external-system, and successor work remain out of scope.
