# Direct provider diagnostics test sequencing

Date: 2026-09-21. Increment: `direct-provider-diagnostics-test-sequencing`.
Status: Required checks passed; ordinary completion requires a valid marker.

## Goal and authorization

The owner explicitly authorized this separately isolated corrective successor
following read-only diagnosis of the terminal failed diagnostic increment.
Correct only its parameterized test scheduling, preserving every later assertion
and all production bytes. Do not reopen, rewrite or reclose either predecessor.
Ordinary begin succeeded in the new worktree after exact candidate transfer.
No gate behavior, admission override or generic failed-state recovery is added.

## Baseline and exact scope

Detached baseline: `0ed15810e90b6a4bd312a8096c61b0abb1ab7eff`.
Copy source: `/private/tmp/cortexa-direct-provider-failure-diagnostics`.
The 32-path candidate includes the failed plan/report and original completion
report. Preserve their bytes. The external preservation manifest records ten
valid predecessors, raw gate files and two already-prunable entries. Do not
inspect credentials, prune entries, modify predecessors or copy their gate state.

The exact 11-path delta against that candidate is:

- `src/features/conversations/PersonalAssistantDirectDemo.test.tsx`
- `CHANGELOG.md`
- `HANDOFF.md`
- `NEXT_STEPS.md`
- `PLANS.md`
- `PROJECT_STATUS.md`
- `ROADMAP.md`
- `TESTING_GUIDE.md`
- `TROUBLESHOOTING_LOG.md`
- `docs/plans/2026-09-21-direct-provider-diagnostics-test-sequencing.md`
- `docs/reviews/2026-09-21-direct-provider-diagnostics-test-sequencing-post-increment-review.md`

The cumulative Git inventory is 34 paths: 32 inherited plus this plan/report.
Validate both inventories separately. All production files, dependencies, policies,
native host/IPC, sample, transport, diagnostics and permissions remain unchanged.

## Correction

The poll effect uses a one-shot 250ms timeout and depends on snapshot updates.
The failed test advanced 500ms within one act, then expected the second response.
Use separate 250ms acts so React renders and rearms between polls, with an explicit
streaming assertion after the first. Preserve the third cleanup poll and all
error, partial-text, disabled/enabled-control and no-extra-request assertions.
No timeout extension, production workaround or generalized test helper is needed.

## Validation

Use key-free subprocesses with OPENAI_API_KEY removed, CORTEXA_OPENAI_DEMO=0,
CARGO_NET_OFFLINE=true and npm_config_offline=true. Reuse local caches through
isolated copy-on-write copies; no install, download, launch or provider request.

1. Focused component/client tests and focused Rust Personal Assistant tests.
2. Full offline `npm run verify` for the inherited cross-boundary diagnostics.
3. `npm run docs:check`, `npm run repository:check`, `npm run security:scan`,
   and `git diff --check` after final documentation.
4. Exact one executable hunk, 11-path delta, 34-path cumulative inventory,
   historical preservation, all predecessor bytes/raw states and registry checks.
5. Session inventory, composed quality reviews and ordinary report finalization,
   status and Stop. Required failure stops without automatic repair or retry.

No fresh dependency resolution or network audit is required for unchanged
package/lock bytes. Native bundle/live verification is explicitly outside this
increment; do not infer it from tests, compile results or Python/OpenSSL TLS.

## Risks, stopping conditions and rollback

Stop on admission rejection, drift, failed required validation, downloads,
sensitive output or expanded scope. Preserve edits without rollback or publication.
If required validation fails, record terminal failure without a completion marker.
One live attempt remains; the task grants no authority to use it. D-125/M1/M2
remain parked. A successful successor cannot erase the predecessor FAIL.

## Progress

Predecessor evidence and exact candidate transfer passed; ordinary admission
passed. The exact one-hunk correction passed all 12 focused frontend cases, including
the three previous failures; all later assertions remain intact. Focused Rust:
41 passed. Full offline verify passed formatting, repository checks, strict lint,
74 hook tests, 83 repository tests, 383 frontend tests, 312 Rust unit tests, all
integration tests, type checking, frontend build and the Tauri no-bundle build.
Documentation/secret/scope/preservation/session checks passed; the final review
records completion evidence. No source edits followed those test runs. The
separate predecessor remains terminal FAIL / Blocked, with its record unchanged.
